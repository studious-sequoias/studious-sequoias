{
  "name": "Tetris",
  "version": "0.0.1",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "mongoose": "^4.7.1"
  },
  "scripts": {
    "start": "pkill node && nodemon server.js 2>&1 | ../log/.predate.sh | tee -a ../log/server.log ",
    "tail": "tail -f ../log/server.log"
  }
}
